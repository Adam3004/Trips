import { IsFiltratedPipe } from './is-filtrated.pipe';

describe('IsFiltratedPipe', () => {
  it('create an instance', () => {
    const pipe = new IsFiltratedPipe();
    expect(pipe).toBeTruthy();
  });
});
